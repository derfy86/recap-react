import React from 'react';
import PropTypes from 'prop-types';
import './style.scss';

const Message = () => (
  <div className="container-message">
    <p>La recherche a donné ? résultats</p>
  </div>
);

Message.propTypes = {

};

export default Message;
